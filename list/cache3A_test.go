package list_test

import (
	"testing"
)

func TestCache3A(t *testing.T) {
	/* asrt := assert.New(t)
	defer func() {
		err := recover()
		asrt.Nil(err)
	}()

		cache := list.NewCache3A[string, struct{}]()
	   	asrt.True(cache.IsEmpty())
	   	asrt.Equal(0, cache.Size()) */

}
